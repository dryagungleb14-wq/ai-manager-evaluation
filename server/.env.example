PORT=3000
CORS_ORIGIN=https://<frontend-on-vercel>.vercel.app
DATABASE_URL=postgres://<user>:<pass>@<host>/<db>
GEMINI_API_KEY=your-google-ai-studio-key
# SESSION_SECRET is REQUIRED for production. Generate a secure random key with:
# openssl rand -hex 32
# This will output something like: a1b2c3d4e5f6...
SESSION_SECRET=REPLACE_WITH_GENERATED_VALUE_FROM_OPENSSL
